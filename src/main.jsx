import React from 'react';
import ReactDOM from 'react-dom/client';
import { FlexiFormPlayground } from './FlexiFormPlayground';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <FlexiFormPlayground />
  </React.StrictMode>,
);